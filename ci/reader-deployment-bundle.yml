---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: onsdigital/dp-concourse-tools-git

inputs:
  - name: artifacts
  - name: revisions
  - name: zebedee

outputs:
  - name: reader-deployment

run:
  path: zebedee/ci/scripts/reader-deployment-bundle.sh
