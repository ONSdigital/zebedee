---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: onsdigital/dp-concourse-tools-git

inputs:
  - name: cms-build-bundle
  - name: reader-build-bundle
  - name: zebedee

outputs:
  - name: target
  - name: zebedee-cms

run:
  path: zebedee/ci/scripts/unpack.sh
