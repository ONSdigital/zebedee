---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: onsdigital/dp-concourse-tools-git

inputs:
  - name: artifacts
  - name: zebedee

outputs:
  - name: reader-build

run:
  path: zebedee/ci/scripts/reader-build-bundle.sh
